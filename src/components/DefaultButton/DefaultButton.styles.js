import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  button: {
    maxWidth: '100%',
    height: 50,
    borderRadius: 8,
    alignItems: 'center',
    justifyContent: 'center',
    marginBottom: 0,
    flexDirection: 'row',
  },
  loader: {
    position: 'absolute',
    right: 10,
    top: 11,
  },
});